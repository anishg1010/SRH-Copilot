# Categorized teaching documents

Function-based folders produced by the pre-processing step
(`copilot.preprocess.apply_mapping`):
learning_objectives / course_module_design / teaching_methods /
assessment_rubrics / responsible_ai / general_core

The teaching agent points here by default (config.teaching_data_dir).
